# Terminal 1 — Backend
cd Multitask_test/backend_2.0
uvicorn app.main:app --reload --port 8000
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# Terminal 2 — Frontend
cd Multitask_test/UX
npm run dev      # → http://localhost:3000